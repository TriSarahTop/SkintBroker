# Default initialization for an AlphaVantage data presenter using the CSV cache
---
provider_type: alphavantage
provider_params:
  reqs_per_minute: 5
cache_type: csv
cache_params:
  cache: cache
  memory_cache_size: 10
