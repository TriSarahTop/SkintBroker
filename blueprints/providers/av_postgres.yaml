# Default initialization for an AlphaVantage data presenter using the postgres cache
---
provider_type: alphavantage
provider_params:
  reqs_per_minute: 5
cache_type: postgres
cache_params:
  postgres_server: localhost
  postgres_username: skintbroker
  postgres_database: skintbroker
  memory_cache_size: 10
